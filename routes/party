var express = require('express');
var router = express.Router();

/* GET users listing. */
router.get('/:partyid', function(req, res) {
  res.render('party', { title: 'Express' , partyid: req.params.partyid });
});


module.exports = router;
